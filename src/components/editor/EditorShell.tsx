import { useState } from 'react';import type { Editor } from '@tiptap/core';import { RichTextToolbar } from './RichTextToolbar';import { RichTextEditor } from './RichTextEditor';import { EditorStatusBar } from './EditorStatusBar';import { wordCountFromText } from '../../lib/writing/wordCount';
export const EditorShell=()=>{const [editor,setEditor]=useState<Editor|null>(null);return <section><RichTextToolbar editor={editor}/><RichTextEditor onReady={(ready)=>setEditor((ready as unknown as Editor)??null)} /><EditorStatusBar words={wordCountFromText(editor?.getText()??'')}/></section>};
